# 用法：复制本文件为 .env，填入真实 key（.env 已被 gitignore，不会上传）
# ⚠️ key 不要截图、不要贴进聊天、不要提交进 git

# ---------- 对话模型（生成回答用） ----------
LLM_API_KEY=在这里填你的key
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# ---------- 向量化模型（建索引 + 查询用） ----------
# 硅基流动 https://cloud.siliconflow.cn 注册即有免费额度，bge-m3 免费
EMBED_API_KEY=在这里填你的key
EMBED_BASE_URL=https://api.siliconflow.cn/v1
EMBED_MODEL=BAAI/bge-m3

# ---------- 知识库位置 ----------
# 指向 Obsidian 库根目录（正斜杠最省事）
NOTES_DIR=C:/Users/你的用户名/Documents/我的知识库

# ---------- 检索质量参数（可不填，用默认值） ----------
# 可信阈值：最高相似度低于它就判定"库里没有"，让 Agent 拒答而不是编造。
# 这个值不拍脑袋定，由 run_eval.py 的分数分布算出来（见 README「阈值怎么定的」）
EVIDENCE_THRESHOLD=0.45
# 召回条数：一次喂给模型的候选块数量
TOP_K=6
